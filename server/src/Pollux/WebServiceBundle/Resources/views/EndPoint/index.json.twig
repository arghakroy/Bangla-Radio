{
    "links": {
        "self": "{{ url('webservice.endpoint') }}",
        "songs": "{{ url('webservice.songs') }}",
        "artists" : "{{ url('webservice.artists') }}",
        "albums" : "{{ url('webservice.albums') }}",
        "playlists" : "{{ url('webservice.playlists') }}",
        "tags" : "{{ url('webservice.tags') }}",
        "currentProduct" : "{{ url('webservice.product', {'productId': currentProduct.id}) }}",
        "subscriptions" : "{{ url('webservice.subscriptions') }}",
        "login" : "{{ url('telenor.authentication.login') }}"
    }
}
