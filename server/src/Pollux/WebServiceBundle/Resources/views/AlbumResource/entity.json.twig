{
    "name": "{{ entity.name }}",
    "artist": "{{ entity.artist }}",
    "year": "{{ entity.year }}",
    "tracks": "{{ entity.tracks }}",
    "links": {
        "self": "{{ url('webservice.album', { albumId: entity.attributes.id }) }}",
        "preview": "{{ entity.art|filterAuthToken }}",
        "songs": "{{ url('webservice.album.songs', { albumId: entity.attributes.id }) }}",
        "artist" : "{{ url('webservice.artist', { artistId: entity.artist.attributes.id }) }}"
    }
}
