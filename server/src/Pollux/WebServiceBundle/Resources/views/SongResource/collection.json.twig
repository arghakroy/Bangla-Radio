{% set size = entities|length %}
[
{% for entity in entities %}
    {{ include('WebServiceBundle:SongResource:entity.json.twig', {'entity': entity}) }}{% if size != loop.index %},{% endif %}
{% endfor %}
]
