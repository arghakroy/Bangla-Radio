{% set size = entities|length %}
[
{% for entity in entities %}
    {{ include('WebServiceBundle:TagResource:entity.json.twig', {'entity': entity}) }}{% if size != loop.index %},{% endif %}
{% endfor %}
]
